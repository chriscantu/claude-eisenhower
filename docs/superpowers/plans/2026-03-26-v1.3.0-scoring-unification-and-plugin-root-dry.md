# v1.3.0: Scoring Unification + Plugin Root DRY Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminate two DRY violations: (1) make `/prioritize` invoke the `match-delegate.ts` CLI for Q3 scoring instead of re-implementing it inline, and (2) extract duplicated `plugin_root` resolution from 5 commands into a shared reference file.

**Architecture:** Both changes are prompt-layer only — editing markdown command files and creating one reference file. No TypeScript, no new tests, no script changes. The plugin_root DRY fix (Task 1) ships first because Task 2 (scoring unification) depends on the resolved `plugin_root` value to invoke the CLI.

**Tech Stack:** Markdown command files, Jest (existing tests only)

**Specs:**
- `docs/specs/plugin-root-resolution-dry-spec.md` (Option B)
- `docs/specs/prioritize-cli-scoring-spec.md`

---

## File Structure

| Action | File | Responsibility |
|--------|------|---------------|
| Create | `skills/core/references/plugin-root-resolution.md` | Canonical plugin_root resolution logic (read config, fallback, warning) |
| Modify | `commands/schedule.md` | Replace inline plugin_root resolution with reference |
| Modify | `commands/execute.md` | Replace inline plugin_root resolution with reference |
| Modify | `commands/delegate.md` | Replace inline plugin_root resolution with reference |
| Modify | `commands/scan-email.md` | Replace inline plugin_root resolution with reference |
| Modify | `commands/review-week.md` | Replace inline plugin_root resolution with reference |
| Modify | `commands/prioritize.md` | Replace Step 4b inline scoring with CLI invocation |
| Modify | `docs/STRUCTURE.md` | Add new reference file to skills/core listing |

---

### Task 1: Create the shared plugin_root resolution reference file

**Files:**
- Create: `skills/core/references/plugin-root-resolution.md`

- [ ] **Step 1: Create the reference file**

Write `skills/core/references/plugin-root-resolution.md` with this exact content:

```markdown
# Plugin Root Resolution

Before invoking any script in `scripts/`, resolve the plugin root path:

1. Read `config/task-output-config.md`
2. Find the `plugin_root` value
3. If present: use it as the base path for all script invocations
4. If absent: use `~/repos/claude-eisenhower` as the default and emit this warning
   to the user:

   > "plugin_root not configured in config/task-output-config.md — using default
   > path ~/repos/claude-eisenhower. Run /setup to configure."

5. Store the resolved path for the remainder of this command's execution.
   All script paths are constructed as: `{plugin_root}/scripts/{script_name}`
```

- [ ] **Step 2: Run tests to confirm no breakage**

Run: `cd scripts && npm test`
Expected: 196 tests passing (no tests touch this new file)

- [ ] **Step 3: Commit**

```
git add skills/core/references/plugin-root-resolution.md
git commit -m "feat: add shared plugin-root resolution reference file

Single source of truth for plugin_root config read + fallback logic.
Commands will reference this instead of duplicating resolution inline."
```

---

### Task 2: Replace inline plugin_root resolution in 5 commands

**Files:**
- Modify: `commands/schedule.md`
- Modify: `commands/scan-email.md`
- Modify: `commands/review-week.md`
- Modify: `commands/execute.md`
- Modify: `commands/delegate.md`

Each command currently has its own inline block for reading `plugin_root` from
`config/task-output-config.md` and implementing fallback logic. Replace each with
a one-line reference to the shared file.

- [ ] **Step 1: Update `commands/schedule.md`**

Read the file. Find the plugin_root resolution block (around lines 203-208, the section that reads `plugin_root` from config and has the fallback to `~/repos/claude-eisenhower`). Replace that block with:

```markdown
Resolve `plugin_root` following `skills/core/references/plugin-root-resolution.md`.
```

Keep all lines that USE `{plugin_root}` (like the `cal_query.swift` invocation). Only remove the lines that DEFINE how to resolve it.

- [ ] **Step 2: Update `commands/scan-email.md`**

Read the file. Find lines 149-151 (the plugin_root resolution in Step 6). Replace:

```
- Read `plugin_root` from `config/task-output-config.md`
- If not present, use `~/repos/claude-eisenhower` and note: "plugin_root not configured — using default."
```

With:

```markdown
Resolve `plugin_root` following `skills/core/references/plugin-root-resolution.md`.
```

- [ ] **Step 3: Update `commands/review-week.md`**

Read the file. Find lines 24-26 (the plugin_root resolution at the top). Replace:

```
Read `plugin_root` from task-output-config.md.
If `plugin_root` is not present, use `~/repos/claude-eisenhower` and note:
`"plugin_root not configured — using default path ~/repos/claude-eisenhower. Update config/task-output-config.md if your installation is at a different location."`
```

With:

```markdown
Resolve `plugin_root` following `skills/core/references/plugin-root-resolution.md`.
```

- [ ] **Step 4: Update `commands/execute.md`**

Read the file. Find line 42 where it reads `plugin_root` from task-output-config. The execute command reads plugin_root as part of a broader config read step. Replace the `plugin_root` portion with a reference line, keeping the adapter and settings reads intact:

```markdown
Resolve `plugin_root` following `skills/core/references/plugin-root-resolution.md`.
```

- [ ] **Step 5: Update `commands/delegate.md`**

Read the file. Find line 60 where it reads `plugin_root` from config. Replace:

```
Read `plugin_root` from `config/task-output-config.md` (required — run /setup if not configured).
```

With:

```markdown
Resolve `plugin_root` following `skills/core/references/plugin-root-resolution.md`.
```

- [ ] **Step 6: Run tests**

Run: `cd scripts && npm test`
Expected: 196 tests passing

- [ ] **Step 7: Grep verification**

Run: `grep -rn '~/repos/claude-eisenhower' commands/`
Expected: Zero results. The hardcoded fallback should only exist in
`skills/core/references/plugin-root-resolution.md`.

- [ ] **Step 8: Commit**

```
git add commands/schedule.md commands/scan-email.md commands/review-week.md commands/execute.md commands/delegate.md
git commit -m "refactor: replace inline plugin_root resolution with shared reference

5 commands now reference skills/core/references/plugin-root-resolution.md
instead of duplicating the config read + fallback logic inline.
Hardcoded ~/repos/claude-eisenhower default now exists in exactly one place."
```

---

### Task 3: Rewrite `/prioritize` Step 4b to invoke the CLI

**Files:**
- Modify: `commands/prioritize.md`

- [ ] **Step 1: Read the current Step 4b**

Read `commands/prioritize.md`. The current Step 4b spans lines 52-79. It contains:
1. Config check (lines 56-59) — reads plugin_root, loads stakeholders.yaml
2. Inline scoring algorithm (lines 61-68) — the prose copy of delegate-core.ts
3. Result surfacing (lines 70-74) — presentation of top scorer
4. Confirmation (lines 76-77) — ask before recording
5. Recording (line 79) — write suggested delegate

- [ ] **Step 2: Replace Step 4b with CLI invocation**

Replace the entire Step 4b section (lines 52-79) with:

```markdown
## Step 4b: For each Q3 task — score delegates via CLI

After classifying a task as Q3, before saving:

1. **Resolve plugin_root** following `skills/core/references/plugin-root-resolution.md`.

2. **Check for stakeholder graph**: Read `{plugin_root}/config/stakeholders.yaml`.

   - If the file does not exist: say "No stakeholder graph found. Copy `config/stakeholders.yaml.example` to `stakeholders.yaml` and fill in your delegates to enable delegation suggestions." Save the task with `Delegate to: [not yet assigned — see stakeholders.yaml]` and continue.
   - If the file exists but the `stakeholders` list is empty: say "Stakeholder graph is empty — no delegates configured." Save with the same placeholder and continue.

3. **Invoke the scoring CLI**:

   ```
   do shell script "cd " & quoted form of "{plugin_root}/scripts" & " && npx ts-node match-delegate.ts " & quoted form of "{task_title}: {task_description}" & " " & quoted form of "" & " 2>&1"
   ```

   Note: the CLI reads `config/stakeholders.yaml` and `memory/glossary.md` relative
   to the working directory. The `cd` ensures it runs from the scripts directory.
   If `memory/glossary.md` does not exist, pending counts default to 0 (no error).

4. **Parse the JSON output**. The CLI returns a JSON object with:
   - `candidates[]` — ranked by score, each with `alias`, `score`, `role`, and `reasons[]`
   - `warnings[]` — advisory messages (e.g., high pending count)
   - `status` — `match`, `no_match`, `empty_graph`, or `no_graph`

5. **Surface results**:
   - `status: match` with one clear top scorer → suggest by alias with reasoning from `reasons[]`
   - `status: match` with tied scores → surface both, prefer `direct_report` on tiebreak, ask user to choose
   - `status: no_match` → say "No clear domain match in your stakeholder graph." Ask: "Who should own this?"
   - If any `warnings[]` mention low capacity → add: "Note: [alias] is currently showing low capacity — confirm they can take this on."
   - If any `warnings[]` mention pending delegations → surface the count as advisory

6. **Ask for confirmation** before recording the delegate — never auto-assign:
   "Does [alias] make sense for this, or would you like to assign someone else?"

7. **Record the result** in the task entry as `Suggested delegate: [alias]` (confirmed at schedule time).
```

- [ ] **Step 3: Verify the prioritize.md inline scoring comment is gone**

Run: `grep -n 'add +3' commands/prioritize.md`
Expected: Zero results. The inline scoring weights should no longer appear.

Run: `grep -n 'match-delegate' commands/prioritize.md`
Expected: One result (the CLI invocation line).

- [ ] **Step 4: Run tests**

Run: `cd scripts && npm test`
Expected: 196 tests passing

- [ ] **Step 5: Commit**

```
git add commands/prioritize.md
git commit -m "feat: /prioritize now invokes match-delegate CLI for Q3 scoring

Replaces inline scoring prose with CLI invocation matching /delegate's pattern.
Enables anti-domain veto and pending-count penalty in /prioritize.
One scoring algorithm, one code path, one test suite."
```

---

### Task 4: Update STRUCTURE.md and verify

**Files:**
- Modify: `docs/STRUCTURE.md`

- [ ] **Step 1: Add new reference file to STRUCTURE.md**

Read `docs/STRUCTURE.md`. Find the `skills/core/references/` listing. Add `plugin-root-resolution.md` to the file list:

```
      plugin-root-resolution.md
```

- [ ] **Step 2: Run full test suite**

Run: `cd scripts && npm test`
Expected: 196 tests passing

- [ ] **Step 3: Final grep verification**

Run all three:

```
grep -rn '~/repos/claude-eisenhower' commands/
```
Expected: Zero results.

```
grep -n 'add +3' commands/prioritize.md
```
Expected: Zero results.

```
grep -n 'match-delegate' commands/prioritize.md
```
Expected: One result (CLI invocation).

- [ ] **Step 4: Commit**

```
git add docs/STRUCTURE.md
git commit -m "docs: add plugin-root-resolution.md to STRUCTURE.md listing"
```

---

### Task 5: CHANGELOG and version bump

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `.claude-plugin/plugin.json`

- [ ] **Step 1: Add v1.3.0 CHANGELOG entry**

Read `CHANGELOG.md`. Add a new entry above the `[v1.2.0]` entry:

```markdown
## [v1.3.0] — 2026-03-26 — Scoring Unification + Plugin Root DRY

Two architectural improvements from codebase review. No user-visible behavior change
beyond more accurate Q3 scoring in `/prioritize`. 196 tests passing.

**Q3 scoring unification:**
- `/prioritize` Step 4b now invokes `match-delegate.ts` CLI instead of inline scoring
- Eliminates untested LLM-arithmetic code path
- Enables anti-domain veto in `/prioritize` (was missing)
- Enables pending-count penalty in `/prioritize` (was missing)
- One scoring algorithm, one code path, one test suite

Spec: `docs/specs/prioritize-cli-scoring-spec.md`

**Plugin root DRY fix:**
- New: `skills/core/references/plugin-root-resolution.md` — canonical resolution logic
- 5 commands (schedule, execute, delegate, scan-email, review-week) now reference
  the shared file instead of duplicating inline resolution + fallback
- Hardcoded `~/repos/claude-eisenhower` default now exists in exactly one place

Spec: `docs/specs/plugin-root-resolution-dry-spec.md`
```

- [ ] **Step 2: Bump version in plugin.json**

Read `.claude-plugin/plugin.json`. Change `"version": "1.1.3"` to `"version": "1.3.0"`.

- [ ] **Step 3: Run tests one final time**

Run: `cd scripts && npm test`
Expected: 196 tests passing

- [ ] **Step 4: Commit**

```
git add CHANGELOG.md .claude-plugin/plugin.json
git commit -m "chore(release): bump version to v1.3.0, add CHANGELOG entry"
```
